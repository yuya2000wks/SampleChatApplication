<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/navigation_home"
    app:startDestination="@id/home">

    <fragment
        android:id="@+id/home"
        android:name="com.example.samplechatapplication.fragment.HomeFragment">
        <action
            android:id="@+id/homeToFriendSearch"
            app:destination="@id/friendSearch" />
    </fragment>

    <fragment
        android:id="@+id/friendSearch"
        android:name="com.example.samplechatapplication.fragment.FriendSearchFragment" />

</navigation>